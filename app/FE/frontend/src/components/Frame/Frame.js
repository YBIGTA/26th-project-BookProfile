import React from "react";
import "./Frame.css";

export const Frame = ({ className }) => {
  return (
    <div className={`frame ${className}`}>
      {/* Frame content will be added here */}
    </div>
  );
};
