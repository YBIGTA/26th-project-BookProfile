name: Test Secrets

on:
  push:
    branches:
      - backend  # backend 브랜치에서 push될 때만 실행
  workflow_dispatch:  # 수동 실행 가능하도록 설정

jobs:
  test_secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check MONGODB_URI Secret
        run: |
          if [[ -z "${{ secrets.MONGODB_URI }}" ]]; then
            echo "❌ MONGODB_URI is NOT set!"
            exit 1
          else
            echo "✅ MONGODB_URI is set!"
          fi
